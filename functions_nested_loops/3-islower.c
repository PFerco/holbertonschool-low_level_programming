#include "main.h"
/**
 * _islower() - Entry point
 * Description: Program description.
 * Return: 0 or 1 All success
 */
int _islower(int i)
{
	int i;

	if (_islower(i) == 0)
		return (0);
	if (_islower(i) == 1)
		return (1);
}
