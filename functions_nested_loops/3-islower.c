#include "main.h"
/**
 * _islower() - Entry point
 * Description: Program description.
 * Return: 0 or 1 All success
 */
int _islower(int i)
{
	if (i >= 'a' && i <= 'z')
		return (1);
	else
		return (0);
}
