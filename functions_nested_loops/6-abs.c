#include "main.h"
/**
 * _abs - Entry point.
 * @n: Variable
 * Description: Program description.
 * Return: n or -n All success
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
