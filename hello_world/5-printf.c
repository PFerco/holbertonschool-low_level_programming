#include <stdio.h>
/**
 * main - Entry point
 * Description: Program that uses printf
 * Return: 0 All succes
 */

int main(void)
{
	printf("\"with proper grammar, but the outcome is a piece of art,");
	return (0);
}
